[project]
name = "headroom-video-demo"
version = "0.1.0"
description = "Demo project for the Headroom YouTube video"
requires-python = ">=3.11"
dependencies = [
  "headroom-ai",
  "tiktoken",
  "openai",
  "python-dotenv",
  "jupyter",
]

[tool.uv]
package = false
